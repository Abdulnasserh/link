version: "3.9"

services:
  # ── Link MCP Server (Post-Discharge Superpowers) ─────────────────────────
  link-mcp-server:
    build:
      context: ./mcp_server
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
